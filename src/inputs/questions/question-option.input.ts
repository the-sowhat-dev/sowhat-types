export interface QuestionOptionInput {
  key: string; // unique key for the option: e.g. "employed", "unemployed", "student", ..
  label: string; // display label for the user: e.g. "Employed", "Unemployed", "Student", ..
}
